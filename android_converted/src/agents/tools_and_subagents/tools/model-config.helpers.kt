@file:Suppress("unused", "UNUSED_PARAMETER", "UNCHECKED_CAST")

/**
 * Phase 2 Kotlin port for `src/agents/tools/model-config.helpers.ts`.
 * Structural translation only; external dependencies remain as placeholders where needed.
 */

// TODO(port-deps): import type { OpenClawConfig } from "../../config/config.js";
// TODO(port-deps): import {
// TODO(port-deps): resolveAgentModelFallbackValues,
// TODO(port-deps): resolveAgentModelPrimaryValue,
// TODO(port-deps): } from "../../config/model-input.js";
// TODO(port-deps): import type { AgentModelConfig } from "../../config/types.agents-shared.js";
// TODO(port-deps): import { ensureAuthProfileStore, listProfilesForProvider } from "../auth-profiles.js";
// TODO(port-deps): import { DEFAULT_MODEL, DEFAULT_PROVIDER } from "../defaults.js";
// TODO(port-deps): import { resolveEnvApiKey } from "../model-auth.js";
// TODO(port-deps): import { resolveConfiguredModelRef } from "../model-selection.js";

typealias ToolModelConfig = Any /* TODO: translate TypeScript alias */
