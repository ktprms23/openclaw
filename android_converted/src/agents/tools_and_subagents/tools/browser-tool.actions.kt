@file:Suppress("unused", "UNUSED_PARAMETER", "UNCHECKED_CAST")

/**
 * Phase 2 Kotlin port for `src/agents/tools/browser-tool.actions.ts`.
 * Structural translation only; external dependencies remain as placeholders where needed.
 */

// TODO(port-deps): import type { AgentToolResult } from "@mariozechner/pi-agent-core";
// TODO(port-deps): import { browserAct, browserConsoleMessages } from "../../browser/client-actions.js";
// TODO(port-deps): import { browserSnapshot, browserTabs } from "../../browser/client.js";
// TODO(port-deps): import { resolveBrowserConfig, resolveProfile } from "../../browser/config.js";
// TODO(port-deps): import { DEFAULT_AI_SNAPSHOT_MAX_CHARS } from "../../browser/constants.js";
// TODO(port-deps): import { getBrowserProfileCapabilities } from "../../browser/profile-capabilities.js";
// TODO(port-deps): import { loadConfig } from "../../config/config.js";
// TODO(port-deps): import { wrapExternalContent } from "../../security/external-content.js";
// TODO(port-deps): import { imageResultFromFile, jsonResult } from "./common.js";

typealias BrowserProxyRequest = Any /* TODO: translate TypeScript alias */
