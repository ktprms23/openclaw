@file:Suppress("unused", "UNUSED_PARAMETER", "UNCHECKED_CAST")

/**
 * Phase 2 Kotlin port for `src/agents/tools/pdf-tool.helpers.ts`.
 * Structural translation only; external dependencies remain as placeholders where needed.
 */

// TODO(port-deps): import type { AssistantMessage } from "@mariozechner/pi-ai";
// TODO(port-deps): import type { OpenClawConfig } from "../../config/config.js";
// TODO(port-deps): import {
// TODO(port-deps): resolveAgentModelFallbackValues,
// TODO(port-deps): resolveAgentModelPrimaryValue,
// TODO(port-deps): } from "../../config/model-input.js";
// TODO(port-deps): import { extractAssistantText } from "../pi-embedded-utils.js";

typealias PdfModelConfig = Any /* TODO: translate TypeScript alias */
