@file:Suppress("unused", "UNUSED_PARAMETER", "UNCHECKED_CAST")

/**
 * Phase 2 Kotlin port for `src/agents/subagent-registry.types.ts`.
 * Structural translation only; external dependencies remain as placeholders where needed.
 */

// TODO(port-deps): import type { DeliveryContext } from "../utils/delivery-context.js";
// TODO(port-deps): import type { SubagentRunOutcome } from "./subagent-announce.js";
// TODO(port-deps): import type { SubagentLifecycleEndedReason } from "./subagent-lifecycle-events.js";
// TODO(port-deps): import type { SpawnSubagentMode } from "./subagent-spawn.js";

typealias SubagentRunRecord = Any /* TODO: translate TypeScript alias */
